Run only the CM05 Boltz2 cloud environment debug. Do not run Boltz2 prediction.

Work in `cloud_run_packages/boltz2_cm05_berberine_priority6_2026-06-20`.

Required command:

```bash
bash run_cloud_env_debug.sh
```

Commit generated `cloud_results/` files. In the final response, report Python 3.11 availability, venv creation, pip install result, boltz availability, disk, and any failure point.
